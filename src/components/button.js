import Button from 'react-bootstrap/Button';

function TypesExample(props) {
    return (
    <>
        <Button variant="primary" className={props.classe}>Clicar!</Button>{' '}

    </>
    );
}

export default TypesExample;